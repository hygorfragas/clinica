# `@schedule-x/calendar@4.5.0` declara peer `temporal-polyfill@0.3.0` exato,
# mas o projeto usa `^0.3.2`. O install local funciona porque o npm é
# permissivo, mas `npm ci` (modo strict, usado no Docker) falha.
# Mantendo o resolution global pra ambos cenários.
legacy-peer-deps=true
